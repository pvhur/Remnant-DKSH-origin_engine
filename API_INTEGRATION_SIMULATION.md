# API 통합 시뮬레이션: 소비자 관점

## 시나리오: 이커머스 웹사이트 운영자 김개발씨

---

## 📋 상황 설정

**이름**: 김개발  
**직업**: 중소 이커머스 웹사이트 운영자  
**문제**: 환불 관리와 고객 분석을 수동으로 처리하고 있어서 시간이 많이 걸림  
**목표**: Origin AI API를 사용하여 자동화하고 데이터를 실시간으로 확인하고 싶음

---

## 🚀 통합 과정 시뮬레이션

### **1일차: API 통합 가이드 발견**

**김개발씨의 행동**:
- Origin AI 대시보드에 로그인
- "API 통합" 탭 클릭
- API 가이드 페이지 확인

**보게 되는 내용**:
```
✅ API 개요 및 시작하기
✅ API 키 관리 섹션
✅ API 엔드포인트 목록
✅ 코드 예제 (JavaScript, Python, cURL)
✅ 단계별 통합 가이드
```

**김개발씨의 생각**:
> "오, 이거 정말 간단해 보이네! 우리 웹사이트에 바로 적용할 수 있을 것 같아."

---

### **2일차: API 키 발급 및 첫 번째 통합**

#### **오전 10시: API 키 발급**

**김개발씨의 행동**:
1. API 통합 탭 → "API 키 관리" 섹션
2. "새 API 키 생성" 버튼 클릭
3. 생성된 API 키 복사: `sk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`

**김개발씨의 생각**:
> "API 키를 안전하게 보관해야겠다. .env 파일에 저장하자."

---

#### **오후 2시: 첫 번째 API 호출 구현**

**김개발씨의 웹사이트 구조**:
```
my-ecommerce-site/
├── src/
│   ├── components/
│   │   └── RefundList.jsx  (새로 만들 예정)
│   ├── services/
│   │   └── originApi.js    (새로 만들 예정)
│   └── .env
```

**김개발씨가 하는 작업**:

**1단계: .env 파일에 API 키 추가**
```bash
# .env
ORIGIN_API_KEY=sk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
ORIGIN_API_BASE_URL=https://api.origin.ai
```

**2단계: API 서비스 파일 생성** (`src/services/originApi.js`)
```javascript
// API 가이드에서 복사한 코드를 약간 수정
const API_KEY = process.env.REACT_APP_ORIGIN_API_KEY;
const API_BASE_URL = process.env.REACT_APP_ORIGIN_API_BASE_URL || 'https://api.origin.ai';

// 환불 목록 조회
export async function getRefunds() {
  const response = await fetch(`${API_BASE_URL}/api/refunds`, {
    method: 'GET',
    headers: {
      'Authorization': `Bearer ${API_KEY}`,
      'Content-Type': 'application/json',
    },
  });
  
  if (!response.ok) {
    throw new Error('API 호출 실패');
  }
  
  const data = await response.json();
  return data;
}

// KPI 데이터 조회
export async function getKPIs() {
  const response = await fetch(`${API_BASE_URL}/api/dashboard/kpis`, {
    method: 'GET',
    headers: {
      'Authorization': `Bearer ${API_KEY}`,
      'Content-Type': 'application/json',
    },
  });
  
  const data = await response.json();
  return data;
}
```

**김개발씨의 생각**:
> "가이드에 있는 코드 예제가 정말 도움이 되네. 복사해서 조금만 수정하면 되겠다."

---

#### **오후 4시: React 컴포넌트에 통합**

**김개발씨가 만드는 컴포넌트** (`src/components/RefundList.jsx`):
```javascript
import React, { useState, useEffect } from 'react';
import { getRefunds } from '../services/originApi';

function RefundList() {
  const [refunds, setRefunds] = useState([]);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState(null);

  useEffect(() => {
    async function fetchRefunds() {
      try {
        setLoading(true);
        const data = await getRefunds();
        setRefunds(data);
      } catch (err) {
        setError(err.message);
      } finally {
        setLoading(false);
      }
    }
    
    fetchRefunds();
  }, []);

  if (loading) return <div>로딩 중...</div>;
  if (error) return <div>에러: {error}</div>;

  return (
    <div>
      <h2>환불 내역</h2>
      {refunds.map(refund => (
        <div key={refund.id} className="refund-item">
          <h3>{refund.orderId}</h3>
          <p>고객: {refund.customerName}</p>
          <p>금액: {refund.amount.toLocaleString()}원</p>
          <p>상태: {refund.status}</p>
          <p>사유: {refund.reason}</p>
        </div>
      ))}
    </div>
  );
}

export default RefundList;
```

**김개발씨의 생각**:
> "이제 우리 관리자 페이지에 이 컴포넌트를 추가하면 되겠다!"

---

### **3일차: 테스트 및 배포**

#### **오전 9시: 로컬 테스트**

**김개발씨의 행동**:
```bash
npm start
```

**결과**:
- ✅ API 호출 성공
- ✅ 환불 데이터가 화면에 표시됨
- ✅ 에러 없음

**김개발씨의 생각**:
> "완벽해! 이제 프로덕션에 배포해도 될 것 같다."

---

#### **오후 2시: 프로덕션 배포**

**김개발씨의 행동**:
1. 프로덕션 환경 변수 설정
2. 코드 배포
3. 실제 웹사이트에서 테스트

**결과**:
- ✅ 프로덕션에서도 정상 작동
- ✅ 환불 데이터가 실시간으로 표시됨

**김개발씨의 생각**:
> "이제 수동으로 환불 내역을 확인할 필요가 없어졌다! 시간이 많이 절약되겠다."

---

### **4일차: 추가 기능 통합**

#### **오전: KPI 대시보드 추가**

**김개발씨의 행동**:
- API 가이드에서 KPI API 엔드포인트 확인
- `getKPIs()` 함수 사용
- 대시보드 컴포넌트에 통합

**결과**:
```javascript
// 대시보드에 추가된 코드
import { getKPIs } from '../services/originApi';

function Dashboard() {
  const [kpis, setKPIs] = useState(null);
  
  useEffect(() => {
    getKPIs().then(setKPIs);
  }, []);
  
  return (
    <div>
      <h2>주요 지표</h2>
      <div>CLV 증가율: {kpis?.clvGrowth}</div>
      <div>환불율: {kpis?.refundRate}</div>
      <div>활성 고객: {kpis?.activeCustomers}</div>
    </div>
  );
}
```

**김개발씨의 생각**:
> "이제 우리 대시보드에 Origin AI의 데이터가 실시간으로 표시되네! 정말 편리하다."

---

## 📊 통합 후 변화

### **Before (통합 전)**
- ❌ 환불 내역을 수동으로 엑셀에 입력
- ❌ 고객 분석 데이터를 일주일에 한 번 확인
- ❌ 시간 소모: 하루 2-3시간

### **After (통합 후)**
- ✅ 환불 내역이 자동으로 웹사이트에 표시
- ✅ 실시간 KPI 데이터 확인 가능
- ✅ 시간 소모: 거의 없음 (자동화)
- ✅ 더 빠른 의사결정 가능

---

## 💡 소비자 관점에서의 장점

### **1. 쉬운 통합**
- API 가이드가 명확하고 코드 예제가 많음
- 복사-붙여넣기로 바로 사용 가능

### **2. 빠른 구현**
- 1-2일 만에 기본 통합 완료
- 추가 기능도 쉽게 확장 가능

### **3. 실시간 데이터**
- 수동 작업 불필요
- 항상 최신 데이터 확인 가능

### **4. 시간 절약**
- 수동 작업 시간 90% 감소
- 더 중요한 업무에 집중 가능

---

## 🎯 최종 결과

**김개발씨의 후기**:
> "처음에는 API 통합이 어려울 줄 알았는데, Origin AI의 가이드가 정말 잘 되어 있어서 
> 하루 만에 기본 통합을 완료할 수 있었어요. 이제 환불 관리와 고객 분석이 훨씬 쉬워졌습니다!"

---

## 📝 체크리스트 (소비자가 따라할 수 있는)

- [ ] Origin AI 대시보드 로그인
- [ ] API 통합 탭 방문
- [ ] API 키 발급
- [ ] 코드 예제 복사
- [ ] 자신의 웹사이트에 통합
- [ ] 테스트
- [ ] 배포

---

**이 시뮬레이션은 실제 소비자가 Origin AI API를 사용하는 과정을 보여줍니다.**
**명확한 가이드와 코드 예제가 있으면 누구나 쉽게 통합할 수 있습니다!**

